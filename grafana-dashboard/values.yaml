dashboard:
  alertManager:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  bosh:
    create: true
    datasource:
      # data source variable is used
      multiple: false
      #if not multiple single data source name is used
      single-datasource-name: bosh

  k8s:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  nodeExporter:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  openebs:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  postgresql:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  rabbitMq:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  redis:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus

  traefik_v1:
    create: true
    datasource:
      # data source variable is used
      multiple: true
      #if not multiple single data source name is used
      single-datasource-name: prometheus


